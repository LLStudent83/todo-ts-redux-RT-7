import React from "react";
import { useTable } from "react-table";
import type { TodoType } from "../../store/todoReducer";

type PropsType = {
  data: TodoType[];
  columns: any[];
};

export default function TableToDo(props: PropsType) {
  return null;
}
